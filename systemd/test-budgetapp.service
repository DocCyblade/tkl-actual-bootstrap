# Licensed under GPL-3.0-or-later — see /opt/tkl-actual-bootstrap/LICENSE or repository root
[Unit]
Description=Actual Budget Sync Server (test)
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
User=budget-server
Group=budget-server
WorkingDirectory=/srv/app/test
Environment=PATH=/srv/app/test/node_modules/.bin:/usr/local/bin:/usr/bin
Environment=NODE_ENV=production
Environment=ACTUAL_DATA_DIR=/srv/test/data
ExecStart=/srv/app/test/node_modules/.bin/actual-server
Restart=on-failure
RestartSec=5s

NoNewPrivileges=true
PrivateTmp=true
ProtectSystem=full
ProtectHome=true
ReadWritePaths=/srv/test/data

[Install]
WantedBy=multi-user.target